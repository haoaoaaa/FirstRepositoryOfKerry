# git

Project repository.

## Getting started

Add your source files to this folder, then commit them:

```bash
git add .
git commit -m "Add project files"
```

## Pushing to GitHub

Set the remote and push once the repository exists on GitHub:

```bash
git remote add origin https://github.com/<username>/<repo>.git
git branch -M main
git push -u origin main
```
